# Тренажёр Ингибиторного Контроля

Профессиональный веб-тренажёр для развития ингибиторного контроля с использованием теста Шульте и комплекса из 6 упражнений.

## 🚀 Возможности

- **Диагностика**: Первичная и повторная диагностика с использованием теста Шульте
- **Тренировка**: 6 упражнений для развития ингибиторного контроля
  - 3 отвлекающих упражнения (отвлекающие стимулы, фланкер, струп-тест)
  - 3 затормаживающих упражнения (Go/No-Go, переключение задач, стоп-сигнал)
- **Анализ результатов**: Визуализация прогресса с графиками и таблицами
- **Сравнение**: Сопоставление результатов до и после тренировок

## 📋 Технологии

- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Backend**: Python 3.8+, FastAPI
- **База данных**: MySQL 8.0+
- **Визуализация**: Chart.js

## 🛠️ Установка и запуск

### Требования

- Python 3.8 или выше
- MySQL 8.0 или выше
- Node.js (опционально, для разработки)

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd contrainer
```

### Шаг 2: Настройка базы данных

1. Создайте базу данных MySQL:
```sql
CREATE DATABASE inhibitory_control;
```

2. Создайте файл `.env` в папке `back/` на основе `.env.example`:
```env
DB_HOST=localhost
DB_NAME=inhibitory_control
DB_USER=root
DB_PASSWORD=your_password
DB_PORT=3306
```

3. Инициализируйте таблицы:
```bash
cd back
python data_base.py
```

### Шаг 3: Установка зависимостей Python

```bash
pip install -r requirements.txt
```

### Шаг 4: Запуск backend сервера

```bash
cd back
python main.py
```

Или с использованием uvicorn:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен по адресу: `http://localhost:8000`

### Шаг 5: Запуск frontend

Откройте файл `front/index.html` в браузере или используйте локальный сервер:

```bash
# С помощью Python
cd front
python -m http.server 8080

# Или с помощью Node.js
npx http-server front -p 8080
```

Frontend будет доступен по адресу: `http://localhost:8080`

## 📖 Использование

### Диагностика

1. Перейдите на страницу "Диагностика"
2. Пройдите тест Шульте 3 раза в состоянии покоя
3. Результаты автоматически сохранятся

### Тренировка

1. Перейдите на страницу "Тестирование"
2. Выберите одно из 6 упражнений
3. Выполните упражнение, следуя инструкциям
4. Результаты автоматически сохранятся

### Повторная диагностика

1. После тренировок перейдите на страницу "Повторная Диагностика"
2. Пройдите тест Шульте 3 раза с отвлекающим видеороликом
3. Результаты автоматически сохранятся

### Результаты

1. Перейдите на страницу "Результаты"
2. Просмотрите графики и таблицы с результатами
3. Сравните первичную и повторную диагностику

## 🗄️ Структура базы данных

### Таблица `diagnostics`
- `id`: Уникальный идентификатор
- `type`: Тип диагностики ('primary' или 'secondary')
- `attempts`: JSON массив с временем каждой попытки
- `average_time`: Среднее время прохождения
- `timestamp`: Дата и время прохождения
- `created_at`: Дата создания записи

### Таблица `training`
- `id`: Уникальный идентификатор
- `exercise_type`: Тип упражнения
- `score`: Набранные очки
- `correct`: Количество правильных ответов
- `incorrect`: Количество неправильных ответов
- `accuracy`: Точность в процентах
- `avg_reaction_time`: Среднее время реакции
- `reaction_times`: JSON массив с временем реакции
- `timestamp`: Дата и время выполнения
- `created_at`: Дата создания записи

## 🌐 API Endpoints

### POST `/api/diagnostics`
Создание записи диагностики

**Тело запроса:**
```json
{
  "type": "primary",
  "attempts": [45.2, 43.1, 44.5],
  "average_time": 44.27,
  "timestamp": "2024-01-01T12:00:00"
}
```

### GET `/api/diagnostics`
Получение всех записей диагностики

### POST `/api/training`
Создание записи тренировки

**Тело запроса:**
```json
{
  "exercise_type": "distraction-1",
  "score": 150,
  "correct": 15,
  "incorrect": 2,
  "accuracy": 88.2,
  "avg_reaction_time": 450.5,
  "reaction_times": [420, 450, 480],
  "timestamp": "2024-01-01T12:00:00"
}
```

### GET `/api/training`
Получение всех записей тренировок

### GET `/api/stats`
Получение статистики

## 🚀 Деплой на сервер

### Backend (FastAPI)

1. Установите зависимости на сервере
2. Настройте переменные окружения
3. Используйте процесс-менеджер (systemd, supervisor, PM2)
4. Настройте reverse proxy (nginx) для обработки запросов

### Frontend

1. Загрузите файлы из папки `front/` на веб-сервер
2. Настройте веб-сервер (nginx, Apache) для обслуживания статических файлов
3. Обновите `API_URL` в JavaScript файлах на адрес вашего backend сервера

### База данных

1. Создайте базу данных на сервере
2. Выполните скрипт инициализации `data_base.py`
3. Настройте права доступа для пользователя базы данных

## 📝 Лицензия

Этот проект создан в рамках учебного задания.

## 👥 Авторы

Разработано для проекта по развитию ингибиторного контроля.

## 🔗 Полезные ссылки

- [Тест Шульте онлайн](https://www.schulte-table.com/)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Chart.js документация](https://www.chartjs.org/)